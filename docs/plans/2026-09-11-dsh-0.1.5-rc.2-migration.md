# dsh 0.1.5-rc.1 → 0.1.5-rc.2 升级计划（yet-another-subagent）

- 日期：2026-09-11
- 走廊：`dsh-v0.1.5-rc.1` (183f08e) → `dsh-v0.1.5-rc.2` (fb2c4b9)
- 卡片来源：oh-my-dsh/dsh-plugin-upgrade-skill `skills/plugin-upgrade/references/v0.1.5-rc.2.md`（6 张草稿卡，DSH-0.1.5-RC2-01…06）
- 回滚基线：`master@9324872`，迁移预期零源码改动

## 升级卡摘要

| 卡 | 类型 | 内容 | 级别 |
|---|---|---|---|
| RC2-01 | breaking | feedback 注入契约：`toggle`→`retract`、`openDialog(messageId, rating)`、`acknowledge()` 移除、`MessageFeedbackToggleResult` 不再导出 | required-if-hit |
| RC2-02 | behavior | 两种评分都改为弹窗确认；提交失败转 6s 警告 toast | required-if-hit |
| RC2-03 | behavior | `FileTypeIcon` 48 个代码分类图标换设计导出 artwork | conditional |
| RC2-04 | behavior | 回合尾动作条与文件区 20/16/20px 间距契约 | optional |
| RC2-05 | behavior | `service-stability` 文案改字，key 不变 | conditional |
| RC2-06 | info | Host 面零变更（334 变更文件中 272 个仅版本号） | informational |

## 本插件触点分析（rg 全量扫描，排除 node_modules/lib/dist/docs）

| 卡 | 结果 | 证据 |
|---|---|---|
| RC2-01/02 | 非命中 | src 无 `dsh-client-ui-message-feedback` 注入契约引用（docs/plans 旧文档除外） |
| RC2-03 | 非命中 | src 无 `FileTypeIcon`/`CodeFileIcon`/`dsh-code-icon-*` 引用 |
| RC2-04 | 非命中 | 不贡献 `conversation.chat.turnTail` |
| RC2-05 | 非命中 | 无 `service-stability` 分类文案复制 |
| RC2-06 | 免疫依据 | Host 面零行为变更；peer 的 `@deepseek-ai/*` 包无改名/移除 |

## 行动

无源码改动（本 edge 全部集中在 Web Client feedback/图标/间距面，插件未触任何面）。生产树升级 rc.2 后复跑三件套抽查。

## 验证

```powershell
pnpm typecheck && pnpm test && pnpm run build
```
